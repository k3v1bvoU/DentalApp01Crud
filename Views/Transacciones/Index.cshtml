@model IEnumerable<dentalApp02.Models.Transaccione>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Monto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Monto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pedido.PedidoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransaccionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TransaccionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TransaccionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
